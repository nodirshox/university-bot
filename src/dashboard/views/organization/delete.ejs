<%- include('../includes/header') %>

<div class="col-md-12">
    <div class="card card-primary">
      <div class="card-header">
        <h3 class="card-title">Confirm</h3>
      </div>
    <form action="/organization/<%= organization._id %>/delete" method="POST">
        <div class="card-body">
        <div class="form-group">
            <label>University</label>
            <input type="text" name="name" class="form-control" value="<%= organization.name %>" autocomplete="off" disabled>
        </div>
        <button type="submit" class="btn btn-danger">Delete</button> <a href="/organization/<%= organization._id %>" class="btn btn-warning"> Cancel</a>
    </div>
    </form>
</div>
</div>

<%- include('../includes/footer') %>
